# Быстрый старт - Электронный экскурсовод

## Минимальная настройка для тестирования

### 1. Настройте конфигурацию

Скопируйте `AiTelegramBot/appsettings.example.json` в `AiTelegramBot/appsettings.json` и заполните:

```json
{
  "Telegram": {
    "BotToken": "ВАШ_TELEGRAM_BOT_TOKEN"
  },
  "YandexGpt": {
    "OAuthToken": "ВАШ_YANDEX_OAUTH_TOKEN",
    "FolderId": "ВАШ_YANDEX_FOLDER_ID"
  },
  "YandexGeocoding": {
    "ApiKey": "ВАШ_YANDEX_GEOCODING_API_KEY"
  },
  "ElevenLabs": {
    "ApiKey": "ВАШ_ELEVENLABS_API_KEY"
  }
}
```

### 2. Получите API ключи (минимум)

**Telegram Bot Token:**
- Напишите [@BotFather](https://t.me/botfather) в Telegram
- Отправьте `/newbot` и следуйте инструкциям
- Скопируйте токен

**Yandex Cloud (YandexGPT + Geocoding):**
- Регистрация: https://cloud.yandex.ru/
- OAuth токен: https://oauth.yandex.ru/authorize?response_type=token&client_id=1a6990aa636648e9b2ef855fa7bec2fb
- В консоли Yandex Cloud создайте папку (folder) и скопируйте Folder ID
- Создайте API ключ для Geocoding API

**ElevenLabs:**
- Регистрация: https://elevenlabs.io/
- Бесплатно: 10,000 символов/месяц
- API ключ в Profile Settings

### 3. Запуск

```bash
cd AiTelegramBot
dotnet restore
dotnet build
dotnet run
```

### 4. Использование в Telegram

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Отправьте `/tour`
4. Нажмите кнопку "📍 Отправить мою геолокацию"
5. Получите экскурсию с аудио!

## Что будет работать:

- ✅ Определение адреса по GPS
- ✅ Генерация исторической справки о месте
- ✅ Аудио-озвучка экскурсии
- ✅ Обычный чат с AI (DeepSeek/OpenAI/YandexGPT)

## Структура проекта:

```
AiTelegramBot/
├── Models/
│   ├── Configuration/       # Настройки сервисов
│   ├── LocationInfo.cs      # Информация о геолокации
│   └── TourGuideResult.cs   # Результат генерации экскурсии
├── Services/
│   ├── YandexGeocodingService.cs  # Геокодинг
│   ├── ElevenLabsService.cs       # TTS
│   └── TourGuideService.cs        # Генератор экскурсий
├── Handlers/
│   ├── CommandHandler.cs    # Команды (/tour, /start, etc.)
│   └── MessageHandler.cs    # Сообщения и геолокация
└── Program.cs              # Точка входа
```

## Возможные проблемы:

**"Не удалось сгенерировать экскурсию":**
- Проверьте API ключи в appsettings.json
- Убедитесь, что у вас есть интернет
- Проверьте лимиты на API (особенно ElevenLabs)

**"Audio generation failed":**
- Проверьте ElevenLabs API ключ
- Проверьте лимит символов (бесплатно: 10k/месяц)
- Экскурсия всё равно будет отправлена текстом

**Бот не отвечает:**
- Проверьте Telegram Bot Token
- Убедитесь, что бот запущен (`dotnet run`)
- Проверьте логи в консоли

## Следующие шаги:

- Добавьте базу достопримечательностей Казани
- Настройте кэширование аудио
- Добавьте фотографии мест
- Реализуйте маршруты между точками

Подробная документация: [TOUR_GUIDE_SETUP.md](TOUR_GUIDE_SETUP.md)
